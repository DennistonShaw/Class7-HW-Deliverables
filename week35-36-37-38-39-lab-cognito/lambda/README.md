# Lambda Directory Notes

This directory contains Lambda source files and deployment packages used across the Cognito lab sequence.

The deployed Lambda handler files are:

```text
lambda/index.py
lambda/index.js
```

The deployment packages are:

```text
lambda/python.zip
lambda/node.zip
```

Other Lambda files in this directory are earlier working copies, helper files, or reference versions from previous weeks.

For Week 39 token tracking, update the deployed handler files:

```text
lambda/index.py
lambda/index.js
```

I'm not going to reorganize this directory during this Week 39 lab as it would break file paths in the previous week builds. File cleanup can be handled later in a separate refactor.